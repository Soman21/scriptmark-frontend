# Local backend during development
VITE_API_URL=http://localhost:4000

# Later, in Vercel's Environment Variables settings, set this to your
# Railway backend URL instead, e.g. https://scriptmark-backend.up.railway.app
